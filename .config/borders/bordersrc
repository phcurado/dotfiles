#!/bin/bash

# JankyBorders — focused window outlined in blue, unfocused none.
options=(
  style=round
  width=10.0
  hidpi=on
  active_color=0xff89b4fa   # Catppuccin Mocha blue (focused)
  inactive_color=0x00000000 # transparent (unfocused)
)

borders "${options[@]}"
